import React, {Component, PropTypes} from 'react';
import {connect} from 'react-redux';
import FindScrimContainer from './FindScrimContainer';

export default {
    path: 'find_pickup',
    component: FindScrimContainer
}